databases:
  - name: devagentshub-postgres
    databaseName: dev_agents_hub
    user: devagentshub
    plan: basic-256mb

services:
  - type: web
    name: devagentshub-api
    env: node
    branch: main
    plan: starter
    autoDeployTrigger: checksPass
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @devagentshub/api prisma:generate && pnpm --filter @devagentshub/api build
    preDeployCommand: pnpm --filter @devagentshub/api prisma:migrate:deploy
    startCommand: pnpm --filter @devagentshub/api start
    healthCheckPath: /health
    buildFilter:
      paths:
        - apps/api/**
        - packages/**
        - package.json
        - pnpm-lock.yaml
        - pnpm-workspace.yaml
        - turbo.json
        - tsconfig.base.json
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: devagentshub-postgres
          property: connectionString
      - key: JWT_SECRET
        sync: false
      - key: CORS_ORIGIN
        sync: false

  - type: web
    name: devagentshub-web
    env: node
    branch: main
    plan: starter
    autoDeployTrigger: checksPass
    buildCommand: pnpm install --frozen-lockfile && pnpm --filter @devagentshub/web build
    startCommand: pnpm --filter @devagentshub/web start
    buildFilter:
      paths:
        - apps/web/**
        - packages/**
        - package.json
        - pnpm-lock.yaml
        - pnpm-workspace.yaml
        - turbo.json
        - tsconfig.base.json
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_API_URL
        sync: false
